---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
  - src/lib/supabase/types.ts
  - src/lib/supabase/db.ts
  - src/lib/__tests__/supabase-db.test.ts
autonomous: false
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05, PERS-06, PERS-07]

must_haves:
  truths:
    - "Dashboard metadata (slug, type, status, client names, agent) persists in Supabase"
    - "Sell dashboard data (property details, comps, metrics, narratives) persists in Supabase"
    - "Buyer dashboard data (search criteria, neighborhoods, schools) persists in Supabase"
    - "Buy/sell dashboards link both sell_data and buy_data to one dashboard record"
    - "Authenticated team members can CRUD all tables"
    - "Anonymous users can SELECT only published dashboards and associated data"
    - "New dashboards save as draft immediately"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Full schema with 4 tables, RLS policies, triggers"
      contains: "CREATE TABLE dashboards"
    - path: "src/lib/supabase/types.ts"
      provides: "TypeScript types for all Supabase tables"
      exports: ["Dashboard", "SellData", "BuyData", "PropertyOfInterest", "DashboardType", "DashboardStatus"]
    - path: "src/lib/supabase/db.ts"
      provides: "CRUD helper functions for dashboard operations"
      exports: ["createDashboard", "updateDashboard", "getDashboard", "listDashboards", "upsertSellData", "upsertBuyData"]
  key_links:
    - from: "src/lib/supabase/db.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createClient for server-side DB operations"
      pattern: "createClient.*supabase/server"
    - from: "src/lib/supabase/db.ts"
      to: "src/lib/supabase/types.ts"
      via: "import Dashboard, SellData, BuyData types"
      pattern: "import.*Dashboard.*types"
    - from: "supabase/schema.sql"
      to: "Supabase dashboard"
      via: "Manual execution in SQL editor"
      pattern: "CREATE TABLE"

user_setup:
  - service: supabase
    why: "Database and auth backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public key"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Run schema.sql in the SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> paste supabase/schema.sql -> Run"
      - task: "Create first team member account"
        location: "Supabase Dashboard -> Authentication -> Users -> Add User (email + password)"
---

<objective>
Create Supabase database schema with 4 tables and RLS policies, plus TypeScript helpers for dashboard CRUD operations.

Purpose: All dashboard data needs to persist across sessions. Currently the app is stateless -- everything is lost on page refresh.
Output: SQL schema file, TypeScript types, and CRUD helper module ready for wizard integration in Phase 2.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing types this plan should be aware of but NOT modify -->
From src/lib/types.ts:
```typescript
export interface ClientDetails {
  folderName: string;
  clientNames: string;
  fullName: string;
  email: string;
  address: string;
  cityStateZip: string;
  subdivision: string;
  communityName: string;
  agentKey: string;
}

export interface CompSale {
  addr: string; sub: string; community: string; close: string;
  sp: number; sf: number; ppsf: number; beds: string; baths: string;
  pool: string; dom: string; yearBuilt: number; stories: number;
  matchScore: number; note: string; url?: string;
}

export interface MarketMetrics {
  medianSoldPrice: number; medianPpsf: number; avgPpsf: number;
  ppsfRange: { low: number; high: number };
  derivedValue: number; derivedRange: { low: number; high: number };
  // ... (see full type in src/lib/types.ts)
}
```

<!-- Supabase client from Plan 01 (parallel, but types are standalone) -->
From src/lib/supabase/server.ts (created by Plan 01):
```typescript
export async function createClient(): Promise<SupabaseClient>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create schema SQL, TypeScript types, and CRUD helpers</name>
  <files>
    supabase/schema.sql
    src/lib/supabase/types.ts
    src/lib/supabase/db.ts
    src/lib/__tests__/supabase-db.test.ts
  </files>
  <behavior>
    - createDashboard inserts a dashboard row with status "draft" and returns the created record
    - getDashboard retrieves a dashboard by id with optional sell_data and buy_data joins
    - listDashboards returns all dashboards ordered by updated_at desc
    - upsertSellData creates or updates sell_data for a dashboard_id
    - upsertBuyData creates or updates buy_data for a dashboard_id
    - updateDashboard updates specific fields on a dashboard record
    - A buy/sell dashboard can have both sell_data and buy_data rows linked to same dashboard_id
    - Dashboard type is constrained to "sell" | "buyer" | "buysell"
    - Dashboard status is constrained to "draft" | "published" | "archived"
  </behavior>
  <action>
**Write tests first (RED):**

`src/lib/__tests__/supabase-db.test.ts`:
- Mock the Supabase client (mock `from().insert()`, `from().select()`, `from().update()`, `from().upsert()`)
- Test: createDashboard calls insert on "dashboards" table with status "draft"
- Test: createDashboard includes client_names, type, slug, agent_key
- Test: getDashboard calls select with eq filter on id
- Test: listDashboards calls select with order by updated_at desc
- Test: upsertSellData calls upsert on "sell_data" with dashboard_id
- Test: upsertBuyData calls upsert on "buy_data" with dashboard_id
- Test: updateDashboard calls update with eq filter on id

**Implement (GREEN):**

1. `supabase/schema.sql` — Use the EXACT schema from 01-RESEARCH.md Pattern 2:
   - `dashboards` table: id (UUID), slug (TEXT UNIQUE), type (CHECK sell/buyer/buysell), status (CHECK draft/published/archived DEFAULT draft), client_names, full_name, email, agent_key (DEFAULT 'josh_jacqui'), created_at, updated_at, published_at, created_by (FK auth.users)
   - `sell_data` table: id, dashboard_id (FK CASCADE), address, city_state_zip, subdivision, community_name, beds, baths, sqft, lot_sqft, year_built, pool, stories, estimated_sale_price, loan_payoff, comps (JSONB), market_metrics (JSONB), property_highlights (JSONB), pricing_strategy, competition (JSONB), market_snapshot (JSONB), prep_items (JSONB), marketing_plan (JSONB), timeline (JSONB), features (JSONB), upgrades (JSONB), cromford_metrics (JSONB), cromford_takeaway, timestamps
   - `buy_data` table: id, dashboard_id (FK CASCADE), target_areas, budget_min/max, beds_min, baths_min, must_haves (JSONB), school_preference, neighborhoods (JSONB), school_districts (JSONB), timeline (JSONB), market_snapshot (JSONB), home_search_url, timestamps
   - `properties_of_interest` table: id, dashboard_id (FK CASCADE), address, price, listing_url, photo_url, notes, sort_order, created_at
   - RLS: Enable on all 4 tables. Team CRUD policy (`authenticated` role, USING true WITH CHECK true). Public read policy (`anon` role, SELECT WHERE status='published' on dashboards; sub-select for related tables).
   - updated_at trigger function + triggers on dashboards, sell_data, buy_data

2. `src/lib/supabase/types.ts`:
   - `DashboardType = "sell" | "buyer" | "buysell"`
   - `DashboardStatus = "draft" | "published" | "archived"`
   - `Dashboard` interface matching the dashboards table columns
   - `SellData` interface matching sell_data table (JSONB fields typed as their actual shapes using existing types from `src/lib/types.ts` where applicable -- e.g., comps as `CompSale[]`, market_metrics as `MarketMetrics`)
   - `BuyData` interface matching buy_data table
   - `PropertyOfInterest` interface matching properties_of_interest table
   - `DashboardWithData` interface that includes optional sell_data and buy_data

3. `src/lib/supabase/db.ts`:
   - Import `createClient` from `./server`
   - `createDashboard(data: { type, client_names, slug, agent_key?, ... })` — inserts with status "draft", returns record
   - `getDashboard(id: string)` — select from dashboards, optionally join sell_data and buy_data
   - `listDashboards()` — select all, order by updated_at desc
   - `updateDashboard(id: string, data: Partial<Dashboard>)` — update specific fields
   - `upsertSellData(dashboard_id: string, data: Partial<SellData>)` — upsert by dashboard_id
   - `upsertBuyData(dashboard_id: string, data: Partial<BuyData>)` — upsert by dashboard_id
   - Each function creates a fresh Supabase client (per-request, not module-level)
   - Handle errors by throwing with descriptive messages
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/supabase-db.test.ts</automated>
  </verify>
  <done>
    - Schema SQL file exists with all 4 tables, RLS policies, and triggers
    - TypeScript types match schema columns
    - CRUD helpers cover create, read, update, upsert for all tables
    - All DB tests pass
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set up Supabase project and run schema</name>
  <files>.env.local</files>
  <action>
Human must complete these steps (Claude cannot access the Supabase dashboard):
1. Create a Supabase project at https://supabase.com/dashboard if not already done
2. Go to SQL Editor in the Supabase dashboard
3. Paste the contents of `supabase/schema.sql` and click Run
4. Verify tables appear: dashboards, sell_data, buy_data, properties_of_interest
5. Go to Authentication -> Users -> Add User to create your team account (email + password)
6. Copy project URL and anon key from Settings -> API
7. Add to `.env.local`:
   - `NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co`
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJ...`
  </action>
  <verify>Confirm `.env.local` contains NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY</verify>
  <done>Supabase project created, schema SQL executed, env vars configured, team account provisioned</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (existing + new DB tests)
- `npx tsc --noEmit` — no type errors
- Schema SQL runs without errors in Supabase SQL editor
- Tables visible in Supabase dashboard Table Editor
</verification>

<success_criteria>
- 4 Supabase tables exist with correct columns and constraints
- RLS policies enable team CRUD and public SELECT on published records
- TypeScript types match the database schema
- CRUD helper functions work against the Supabase client
- All DB tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>

---
phase: 04-full-dashboard-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/types.ts
  - src/lib/types.ts
  - src/lib/schemas/dashboard.ts
  - src/lib/publish.ts
  - src/lib/template-sell.html
  - src/lib/template-buyer.html
  - src/lib/template-buysell.html
  - src/components/wizard/StepClientInfo.tsx
  - src/app/api/dashboard/[id]/route.ts
  - src/lib/__tests__/publish.test.ts
autonomous: true
requirements: [TYPE-01, TYPE-02, TYPE-03, TYPE-04]

must_haves:
  truths:
    - "Sell dashboard renders with 4 tabs: Your Home, Market, Listing Plan, Team"
    - "Buyer dashboard renders with 4 tabs: Your Search, Neighborhoods, Properties, Team"
    - "Buy/sell dashboard renders with 5 tabs: Sell Side, Buy Side, Strategy, Properties, Team"
    - "Sell dashboard shows listing status badge (Pre-Listing/Active/Pending/Closed)"
    - "Listing status is selectable in the wizard and persists to Supabase"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "listingStatus field on SellDashboardConfig"
      contains: "listingStatus"
    - path: "src/lib/supabase/types.ts"
      provides: "listing_status field on SellData"
      contains: "listing_status"
    - path: "src/lib/template-sell.html"
      provides: "Listing status badge rendering"
      contains: "listingStatus"
    - path: "src/lib/template-buyer.html"
      provides: "Renamed tabs: Your Search, Neighborhoods, Properties, Team"
      contains: "Your Search"
    - path: "src/lib/template-buysell.html"
      provides: "Renamed tabs: Sell Side, Buy Side, Strategy, Properties, Team"
      contains: "Sell Side"
  key_links:
    - from: "src/lib/publish.ts"
      to: "src/lib/types.ts"
      via: "listingStatus mapped from sell_data.listing_status"
      pattern: "listingStatus.*listing_status"
    - from: "src/components/wizard/StepClientInfo.tsx"
      to: "src/app/api/dashboard/[id]/route.ts"
      via: "PATCH with listing_status in sell_data"
      pattern: "listing_status"
---

<objective>
Align all three dashboard template tab structures with requirements and add listing status badge to sell dashboards.

Purpose: The buyer and buysell templates have tab names that don't match the product spec, and sell dashboards have no way to communicate listing status (Pre-Listing/Active/Pending/Closed) to clients.

Output: Updated template HTML files with correct tab names, listing status field end-to-end (DB type -> Zod schema -> CONFIG type -> publish mapping -> template rendering -> wizard UI selector), and updated tests.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-dashboard-types/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/supabase/types.ts:
```typescript
export interface SellData {
  // ... existing fields ...
  // NOTE: listing_status does NOT exist yet -- must be added
}

export interface DashboardWithData extends Dashboard {
  sell_data?: SellData | null;
  buy_data?: BuyData | null;
}
```

From src/lib/types.ts:
```typescript
export interface SellDashboardConfig {
  templateType: "sell";
  // ... existing fields ...
  // NOTE: listingStatus does NOT exist yet -- must be added
}
```

From src/lib/schemas/dashboard.ts:
```typescript
export const SellDashboardConfigSchema = z.object({
  templateType: z.literal("sell"),
  // ... existing fields ...
  // NOTE: listingStatus not in schema yet
});
```

From src/lib/publish.ts:
```typescript
export function buildConfigFromDashboard(dashboard: DashboardWithData): AnyDashboardConfig;
export async function renderDashboardHtml(dashboardId: string): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add listing status field end-to-end and rename template tabs</name>
  <files>
    src/lib/supabase/types.ts,
    src/lib/types.ts,
    src/lib/schemas/dashboard.ts,
    src/lib/publish.ts,
    src/lib/template-sell.html,
    src/lib/template-buyer.html,
    src/lib/template-buysell.html,
    src/components/wizard/StepClientInfo.tsx,
    src/app/api/dashboard/[id]/route.ts
  </files>
  <action>
    **Listing status field (TYPE-04):**

    1. In `src/lib/supabase/types.ts`, add to `SellData` interface:
       `listing_status?: "pre-listing" | "active" | "pending" | "closed" | null;`

    2. In `src/lib/types.ts`, add to `SellDashboardConfig` interface:
       `listingStatus: "pre-listing" | "active" | "pending" | "closed";`

    3. In `src/lib/schemas/dashboard.ts`, add to `SellDashboardConfigSchema`:
       `listingStatus: z.enum(["pre-listing", "active", "pending", "closed"])`

    4. In `src/lib/publish.ts` `buildConfigFromDashboard()`:
       - In the `type === "sell"` branch, add: `listingStatus: sd?.listing_status ?? "pre-listing"`
       - In the `buysell` branch, add: `sellListingStatus: sd?.listing_status ?? "pre-listing"` -- also add this field to BuySellDashboardConfig type and schema.

    5. In `src/lib/template-sell.html`:
       - In the render() function, add a status badge to the header area or top of the "Your Home" tab. Use `CONFIG.listingStatus` to render a colored badge.
       - Badge styles: pre-listing (gray/neutral), active (green), pending (amber/gold), closed (olive). Reuse existing `.status-badge` CSS pattern but add new classes `.status-pre-listing`, `.status-active-listing`, `.status-pending-listing`, `.status-closed-listing`.
       - Badge text should be title-cased: "Pre-Listing", "Active", "Pending", "Closed".

    6. In `src/components/wizard/StepClientInfo.tsx`:
       - Add a "Listing Status" dropdown select for sell and buysell dashboard types.
       - Options: Pre-Listing (default), Active, Pending, Closed.
       - Value stored as `listing_status` in the sell_data PATCH payload.
       - Only show this dropdown when type is "sell" or "buysell".

    7. In `src/app/api/dashboard/[id]/route.ts`:
       - Ensure the PATCH handler passes `listing_status` through when included in `sell_data` updates. The existing pattern already handles nested sell_data objects -- verify listing_status is not filtered out.

    **Template tab renames:**

    8. In `src/lib/template-buyer.html`:
       - Rename tab button text: "Home Search" -> "Your Search", "Neighborhoods & Schools" -> "Neighborhoods", "Timeline" -> "Properties"
       - Update ALL three locations for each rename: (a) button text, (b) `showTab('newid')` onclick, (c) `id="tab-newid"` on panel div
       - Specifically: `showTab('search')` -> `showTab('search')` (keep ID), `showTab('neighborhoods')` -> `showTab('neighborhoods')` (keep ID), `showTab('timeline')` -> `showTab('properties')` (change ID)
       - Move timeline content (buying process steps) into the "Your Search" tab as a collapsible section at the bottom. Schools content stays in Neighborhoods tab.
       - The "Properties" tab panel will be empty for now (Plan 02 will add POI rendering).
       - Add placeholder content for the Properties tab: "No Properties Selected Yet / Your agent will add properties here as you explore options together."

    9. In `src/lib/template-buysell.html`:
       - Rename tab buttons: "Home Search" -> "Buy Side", "Selling" -> "Sell Side", "Schools" -> "Strategy", "Strategy" -> "Properties"
       - Update all three locations per rename (button text, showTab ID, panel div ID)
       - Reorder so Sell Side appears first: tab order should be Sell Side, Buy Side, Strategy, Properties, Team
       - Move schools content into the Buy Side tab (as a section within buy-side content)
       - Move existing strategy content to the Strategy tab
       - The "Properties" tab panel will be empty for now (Plan 02 adds POI rendering).
       - Add same placeholder content as buyer template.
       - Also add `sellListingStatus` badge rendering to the Sell Side tab header, same pattern as sell template.

    **Sell template verification (TYPE-01):**

    10. Verify `src/lib/template-sell.html` already has tabs: Your Home, Market, Listing Plan, Team. Per research these already match -- do NOT rename. Only add the listing status badge (step 5 above).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/publish.test.ts</automated>
  </verify>
  <done>
    - SellData type has listing_status field
    - SellDashboardConfig and BuySellDashboardConfig have listingStatus/sellListingStatus fields
    - Zod schemas validate the new fields
    - buildConfigFromDashboard maps listing_status for sell and buysell types
    - Sell template renders listing status badge from CONFIG.listingStatus
    - Buyer template has tabs: Your Search, Neighborhoods, Properties, Team
    - Buysell template has tabs: Sell Side, Buy Side, Strategy, Properties, Team
    - StepClientInfo shows listing status dropdown for sell/buysell types
    - All existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for listing status and tab structure verification</name>
  <files>src/lib/__tests__/publish.test.ts</files>
  <behavior>
    - Test: buildConfigFromDashboard for sell type includes listingStatus defaulting to "pre-listing" when sell_data.listing_status is null
    - Test: buildConfigFromDashboard for sell type maps listing_status "active" correctly
    - Test: buildConfigFromDashboard for buysell type includes sellListingStatus
    - Test: SellDashboardConfigSchema validates listingStatus enum values
    - Test: SellDashboardConfigSchema rejects invalid listingStatus values
  </behavior>
  <action>
    Add new test cases to the existing `src/lib/__tests__/publish.test.ts` file:

    1. In the "buildConfigFromDashboard" describe block, add tests:
       - "sell config defaults listingStatus to pre-listing when null": create dashboard with sell_data where listing_status is null/undefined, assert config.listingStatus === "pre-listing"
       - "sell config maps listing_status correctly": create dashboard with sell_data where listing_status is "active", assert config.listingStatus === "active"
       - "buysell config includes sellListingStatus": create buysell dashboard, assert config.sellListingStatus exists and defaults to "pre-listing"

    2. Add a new describe block for schema validation:
       - Import validateConfig and SellDashboardConfigSchema from schemas/dashboard
       - Test that valid listingStatus values pass validation
       - Test that invalid values (e.g., "unknown") fail validation

    Update the existing sellData fixture to include `listing_status: "pre-listing"` so existing tests don't break.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/publish.test.ts</automated>
  </verify>
  <done>
    - At least 5 new test cases covering listing status mapping and validation
    - All tests pass including existing ones
    - Tests verify both default behavior and explicit value mapping
  </done>
</task>

</tasks>

<verification>
- `npm test` passes with all existing + new tests green
- Sell template HTML contains listing status badge rendering code
- Buyer template has 4 tab buttons with text: Your Search, Neighborhoods, Properties, Team
- Buysell template has 5 tab buttons with text: Sell Side, Buy Side, Strategy, Properties, Team
- `listingStatus` field exists in SellDashboardConfig type, Zod schema, and publish mapping
</verification>

<success_criteria>
- TYPE-01: Sell dashboard tab structure verified (was already correct)
- TYPE-02: Buyer template tabs renamed to Your Search, Neighborhoods, Properties, Team
- TYPE-03: Buysell template tabs renamed to Sell Side, Buy Side, Strategy, Properties, Team
- TYPE-04: Listing status badge renders on sell dashboards with correct styling per status value
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-dashboard-types/04-01-SUMMARY.md`
</output>

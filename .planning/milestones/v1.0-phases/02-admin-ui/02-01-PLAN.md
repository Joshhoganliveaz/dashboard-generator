---
phase: 02-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/slug.ts
  - src/lib/__tests__/slug.test.ts
  - src/app/page.tsx
  - src/components/library/DashboardCard.tsx
  - src/components/library/LibraryFilters.tsx
  - src/components/library/DashboardLibrary.tsx
  - src/__tests__/library.test.ts
  - src/app/api/slug/check/route.ts
autonomous: true
requirements: [SLUG-01, SLUG-02, SLUG-03, SLUG-04, SLUG-05, LIBR-01, LIBR-02, LIBR-03, LIBR-04, LIBR-05]

must_haves:
  truths:
    - "Slugs are auto-generated from client names + address as lowercase-hyphenated strings"
    - "Slug collisions append -2, -3 suffixes automatically"
    - "Slugs are editable before first publish and locked after"
    - "Team member sees all dashboards as filterable cards on the home screen"
    - "Cards show client name, address/area, type badge, status badge, last updated"
    - "Team member can filter by type and status"
    - "Clicking a card navigates to the wizard for that dashboard"
  artifacts:
    - path: "src/lib/slug.ts"
      provides: "Slug generation and collision detection"
      exports: ["generateSlug", "findAvailableSlug"]
    - path: "src/app/page.tsx"
      provides: "Library home page (server component)"
      min_lines: 10
    - path: "src/components/library/DashboardLibrary.tsx"
      provides: "Client-side filterable dashboard grid"
      min_lines: 30
    - path: "src/components/library/DashboardCard.tsx"
      provides: "Individual dashboard card with badges"
      min_lines: 20
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/supabase/db.ts"
      via: "listDashboards() server-side call"
      pattern: "listDashboards"
    - from: "src/components/library/DashboardCard.tsx"
      to: "/dashboard/[id]/wizard"
      via: "Next.js Link"
      pattern: "href.*dashboard.*wizard"
---

<objective>
Build the slug utility (generation, sanitization, collision detection) and replace the existing single-page form home screen with a dashboard library view showing filterable cards.

Purpose: Provides the two foundational pieces for the admin UI -- slug management for permanent URLs and a home screen for browsing/accessing all dashboards.
Output: Slug utility module with tests, library page with card grid, type/status filters, and a slug check API route.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-ui/02-RESEARCH.md

@src/lib/supabase/types.ts
@src/lib/supabase/db.ts
@src/lib/supabase/server.ts

<interfaces>
<!-- Key types the executor needs from Phase 1 -->

From src/lib/supabase/types.ts:
```typescript
export type DashboardType = "sell" | "buyer" | "buysell";
export type DashboardStatus = "draft" | "published" | "archived";

export interface Dashboard {
  id: string;
  slug: string;
  type: DashboardType;
  status: DashboardStatus;
  client_names: string;
  full_name?: string | null;
  email?: string | null;
  agent_key: string;
  created_at: string;
  updated_at: string;
  published_at?: string | null;
  created_by?: string | null;
}
```

From src/lib/supabase/db.ts:
```typescript
export async function listDashboards(): Promise<Dashboard[]>;
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): // creates per-request Supabase server client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slug utility with TDD + slug check API route</name>
  <files>src/lib/slug.ts, src/lib/__tests__/slug.test.ts, src/app/api/slug/check/route.ts</files>
  <behavior>
    - generateSlug("John Smith", "123 Main St") returns "john-smith-123-main-st"
    - generateSlug strips special characters: "O'Brien & Co" -> "obrien-co"
    - generateSlug truncates to 80 chars max
    - generateSlug with only clientNames (no address) works: "Jane Doe" -> "jane-doe"
    - Slugs contain only lowercase letters, numbers, and hyphens (regex: /^[a-z0-9-]+$/)
    - findAvailableSlug returns base slug when no collision exists
    - findAvailableSlug returns "slug-2" when "slug" exists, "slug-3" when "slug" and "slug-2" exist
    - findAvailableSlug excludeId parameter skips the dashboard's own slug in collision check
  </behavior>
  <action>
    1. Create `src/lib/__tests__/slug.test.ts` with tests for all behaviors above. Mock Supabase for collision tests.
    2. Run tests (expect RED).
    3. Create `src/lib/slug.ts` with:
       - `generateSlug(clientNames: string, address?: string): string` -- concatenate, lowercase, replace non-alphanumeric with hyphens, trim leading/trailing hyphens, truncate to 80 chars
       - `findAvailableSlug(baseSlug: string, excludeId?: string): Promise<string>` -- query `dashboards` table for matching slugs, append -2, -3, etc. until available
    4. Run tests (expect GREEN).
    5. Create `src/app/api/slug/check/route.ts` with GET handler: accepts `?slug=X&excludeId=Y` query params, calls `findAvailableSlug`, returns `{ available: boolean, suggestion: string }`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/slug.test.ts</automated>
  </verify>
  <done>generateSlug produces valid slugs, findAvailableSlug detects collisions and appends suffixes, API route returns availability check. All slug tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard library page with filterable card grid</name>
  <files>src/app/page.tsx, src/components/library/DashboardCard.tsx, src/components/library/LibraryFilters.tsx, src/components/library/DashboardLibrary.tsx, src/__tests__/library.test.ts</files>
  <action>
    1. **Preserve the old page.tsx:** Rename `src/app/page.tsx` to `src/app/legacy/page.tsx` (move old single-page form to /legacy route so houseversary generation still works if needed). Create the legacy directory first.

    2. **Create `src/components/library/DashboardCard.tsx`** ("use client"):
       - Accept `dashboard: Dashboard` prop
       - Show type badge (Sell=terra, Buyer=sage, Buy/Sell=sand) and status badge (Draft=slate, Published=sage-dark, Archived=sand-pale)
       - Show `client_names` as title, `slug` as subtitle (or `target_areas` for buyer type if slug is a draft placeholder)
       - Show "Updated {relative date}" at bottom
       - Wrap in `<Link href={/dashboard/${dashboard.id}/wizard}>` for click-to-edit (LIBR-05)
       - Use existing Tailwind brand colors from the project

    3. **Create `src/components/library/LibraryFilters.tsx`** ("use client"):
       - Type filter: buttons for All / Sell / Buyer / Buy/Sell
       - Status filter: buttons for All / Draft / Published / Archived
       - Accept `onTypeChange(type: DashboardType | null)` and `onStatusChange(status: DashboardStatus | null)` callbacks
       - Active filter gets highlighted styling

    4. **Create `src/components/library/DashboardLibrary.tsx`** ("use client"):
       - Accept `dashboards: Dashboard[]` prop
       - Internal state for typeFilter and statusFilter
       - Filter dashboards client-side by type and status
       - Render LibraryFilters at top, then grid of DashboardCards (3 cols on lg, 2 on md, 1 on sm)
       - Show "No dashboards found" empty state
       - Include a "New Dashboard" button/link that navigates to `/dashboard/new`

    5. **Create new `src/app/page.tsx`** (server component):
       - Import `listDashboards` from `@/lib/supabase/db`
       - Import `DashboardLibrary` component
       - Fetch dashboards server-side, pass to `DashboardLibrary`
       - Page title: "Dashboard Library" or "Dashboards"

    6. **Create `src/__tests__/library.test.ts`:**
       - Test DashboardLibrary filter logic: all dashboards shown when no filter, only sell shown when type=sell, only draft shown when status=draft
       - Test DashboardCard renders client_names, type badge, status badge
       - Mock Dashboard objects for test data
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/library.test.ts</automated>
  </verify>
  <done>Home page shows all dashboards as cards with type/status badges. Filters narrow results by type and status. Each card links to /dashboard/[id]/wizard. Old form preserved at /legacy. Library tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/slug.test.ts` -- all slug generation and collision tests pass
- `npx vitest run src/__tests__/library.test.ts` -- all library filter and card tests pass
- `npm test` -- full suite still green (no regressions from page.tsx replacement)
</verification>

<success_criteria>
- Slug utility generates valid slugs, detects collisions, and has an API route for checking availability
- Home page displays dashboard cards with client name, type badge, status badge, and last updated
- Type and status filters work to narrow the card grid
- Clicking a card navigates to /dashboard/[id]/wizard
- Old single-page form accessible at /legacy for backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-ui/02-01-SUMMARY.md`
</output>

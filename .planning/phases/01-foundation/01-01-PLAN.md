---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/middleware.ts
  - src/app/login/page.tsx
  - src/app/api/login/route.ts
  - src/lib/__tests__/supabase-auth.test.ts
  - src/__tests__/middleware.test.ts
  - src/__tests__/legacy-auth-removed.test.ts
  - .env.local.example
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Team member can log in with email and password"
    - "Session persists across browser refresh (cookie-based JWT)"
    - "Unauthenticated visitors are redirected to /login"
    - "Legacy SITE_PASSWORD auth no longer works"
    - "Josh provisions accounts via Supabase dashboard (no code needed)"
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client"
      exports: ["createClient"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "Middleware Supabase client helper"
      exports: ["createClient"]
    - path: "src/middleware.ts"
      provides: "Auth redirect middleware"
      contains: "getClaims"
    - path: "src/app/login/page.tsx"
      provides: "Email+password login form"
      contains: "signInWithPassword"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import createClient"
      pattern: "createClient.*supabase/middleware"
    - from: "src/app/login/page.tsx"
      to: "src/lib/supabase/client.ts"
      via: "import createClient for signInWithPassword"
      pattern: "signInWithPassword"
    - from: "src/middleware.ts"
      to: "/login redirect"
      via: "getClaims() failure triggers redirect"
      pattern: "getClaims.*redirect.*login"
---

<objective>
Replace the legacy SITE_PASSWORD cookie auth with Supabase email/password authentication.

Purpose: Team members need secure, session-persistent login. The legacy auth is a single shared password stored in an env var -- insecure and not user-specific.
Output: Working Supabase auth with login page, middleware protection, and session persistence.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing middleware to be replaced -->
From src/middleware.ts:
```typescript
// Current: checks "dashboard-auth" cookie === "authenticated"
// Replace with: Supabase getClaims() validation
export function middleware(request: NextRequest)
```

From src/app/api/login/route.ts:
```typescript
// Current: POST handler checking SITE_PASSWORD env var
// Action: DELETE this file entirely
export async function POST(request: Request)
```

From src/app/login/page.tsx:
```typescript
// Current: single password field, POSTs to /api/login
// Replace with: email + password fields using Supabase signInWithPassword
export default function LoginPage()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase deps and create client utilities</name>
  <files>
    src/lib/supabase/client.ts
    src/lib/supabase/server.ts
    src/lib/supabase/middleware.ts
    .env.local.example
  </files>
  <action>
Install `@supabase/supabase-js` and `@supabase/ssr`:
```bash
npm install @supabase/supabase-js @supabase/ssr
```

Create three Supabase client utilities following the exact patterns from 01-RESEARCH.md Pattern 1:

1. `src/lib/supabase/client.ts` — Browser client using `createBrowserClient` from `@supabase/ssr`. Reads `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` from env.

2. `src/lib/supabase/server.ts` — Server client using `createServerClient` from `@supabase/ssr`. Uses `cookies()` from `next/headers`. Must create client per-request (not module-level). The `setAll` callback wraps in try/catch (Server Components can't set cookies).

3. `src/lib/supabase/middleware.ts` — Middleware client using `createServerClient` from `@supabase/ssr`. Takes `request: NextRequest` and returns `{ supabase, response }`. Writes cookies to BOTH the request (for downstream server components) AND the response (for browser). Follow the exact cookie propagation pattern from research to avoid Cloudflare Workers issues.

4. Update `.env.local.example` — Add `NEXT_PUBLIC_SUPABASE_URL=`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=`, and keep existing vars. Remove `SITE_PASSWORD` line if present.

Do NOT use `getSession()` anywhere — always use `getClaims()` for auth validation (per research: getSession does not revalidate JWT).
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Three Supabase client files compile without errors. .env.local.example updated with Supabase vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace middleware, login page, and remove legacy auth</name>
  <files>
    src/middleware.ts
    src/app/login/page.tsx
    src/app/api/login/route.ts
    src/lib/__tests__/supabase-auth.test.ts
    src/__tests__/middleware.test.ts
    src/__tests__/legacy-auth-removed.test.ts
  </files>
  <behavior>
    - Middleware redirects to /login when getClaims() returns error/null
    - Middleware allows /login, /_next, /favicon, /d/* (published dashboards), *.html
    - Middleware passes through when getClaims() succeeds (authenticated)
    - Middleware refreshes token via cookie propagation on each request
    - Login page renders email + password fields
    - Login page calls supabase.auth.signInWithPassword and redirects to / on success
    - Login page shows error message on auth failure
    - Legacy /api/login route no longer exists (file deleted)
    - No reference to SITE_PASSWORD or "dashboard-auth" cookie in codebase
  </behavior>
  <action>
**Write tests first (RED):**

1. `src/__tests__/middleware.test.ts`:
   - Mock `@supabase/ssr` createServerClient
   - Test: unauthenticated request (getClaims returns error) redirects to /login
   - Test: authenticated request passes through
   - Test: /login path is allowed without auth
   - Test: /d/some-slug path is allowed without auth (published dashboards)
   - Test: /_next paths are allowed without auth

2. `src/__tests__/legacy-auth-removed.test.ts`:
   - Test: `src/app/api/login/route.ts` file does NOT exist (use fs.existsSync)
   - Test: no file in src/ contains the string "SITE_PASSWORD" (use grep-like check)
   - Test: no file in src/ contains the string "dashboard-auth" cookie name

3. `src/lib/__tests__/supabase-auth.test.ts`:
   - Mock Supabase client
   - Test: signInWithPassword is called with email + password
   - Test: createBrowserClient receives correct env vars

**Implement (GREEN):**

1. Rewrite `src/middleware.ts`:
   - Import createClient from `@/lib/supabase/middleware`
   - Allow public routes: /login, /_next, /favicon, /d/*, *.html
   - Call `supabase.auth.getClaims()` — redirect to /login on error/null
   - Return the supabase response (carries refreshed cookies)
   - Keep existing matcher config

2. Rewrite `src/app/login/page.tsx`:
   - "use client" component with email + password fields
   - Import createClient from `@/lib/supabase/client`
   - On submit: call `supabase.auth.signInWithPassword({ email, password })`
   - On success: `router.push("/")` + `router.refresh()`
   - On error: display error.message
   - Style with existing Tailwind classes (match current branding: Olive/Canyon/Gold/Cream)

3. DELETE `src/app/api/login/route.ts` entirely.

AUTH-04 (provisioning): No code needed. Josh uses the Supabase dashboard Authentication tab to create user accounts. Document this in a code comment in the login page.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/middleware.test.ts src/__tests__/legacy-auth-removed.test.ts src/lib/__tests__/supabase-auth.test.ts</automated>
  </verify>
  <done>
    - Middleware uses Supabase getClaims() for auth checks
    - Login page accepts email + password via Supabase signInWithPassword
    - /api/login route is deleted
    - No references to SITE_PASSWORD or dashboard-auth cookie remain
    - All auth tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (existing + new auth tests)
- `npx tsc --noEmit` — no type errors
- Manual: After configuring Supabase env vars, login page shows email + password fields
</verification>

<success_criteria>
- Supabase client utilities exist for browser, server, and middleware contexts
- Middleware redirects unauthenticated users to /login via getClaims()
- Login page uses signInWithPassword with email + password
- Legacy /api/login route is deleted
- No SITE_PASSWORD or dashboard-auth references in src/
- All new auth tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

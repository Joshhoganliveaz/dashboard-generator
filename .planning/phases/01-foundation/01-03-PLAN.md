---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/claude-api.ts
  - src/lib/csv-engine.ts
  - src/lib/schemas/mls-extraction.ts
  - src/lib/schemas/dashboard.ts
  - src/lib/__tests__/claude-api.test.ts
  - src/lib/__tests__/csv-engine.test.ts
  - src/lib/__tests__/config-validation.test.ts
autonomous: true
requirements: [ENGN-01, ENGN-02, ENGN-03, ENGN-04, ENGN-05, ENGN-06, ENGN-07]

must_haves:
  truths:
    - "Claude API calls use 16K+ max_tokens for analysis"
    - "Claude extraction uses structured output with guaranteed JSON schema compliance"
    - "Claude API retries on 429 and 5xx with exponential backoff + jitter"
    - "CSV parsing uses Papaparse instead of hand-rolled parseCSVLine"
    - "Comp scoring and metric calculation are fully deterministic (no Claude involvement)"
    - "Claude only does narrative generation and comp validation in the CSV pipeline"
    - "CONFIG is validated against Zod schemas before template injection"
  artifacts:
    - path: "src/lib/claude-api.ts"
      provides: "Refactored Claude API with SDK, structured output, retry"
      exports: ["callClaudeWithRetry", "extractMLSData", "askClaude"]
    - path: "src/lib/csv-engine.ts"
      provides: "Deterministic CSV pipeline with Papaparse"
      contains: "Papa.parse"
    - path: "src/lib/schemas/mls-extraction.ts"
      provides: "Zod schema for MLS PDF extraction"
      exports: ["MLSExtractionSchema"]
    - path: "src/lib/schemas/dashboard.ts"
      provides: "Zod schemas for dashboard config validation"
      exports: ["SellDashboardConfigSchema", "BuyerDashboardConfigSchema"]
  key_links:
    - from: "src/lib/claude-api.ts"
      to: "@anthropic-ai/sdk"
      via: "import Anthropic, zodOutputFormat"
      pattern: "import.*anthropic"
    - from: "src/lib/claude-api.ts"
      to: "src/lib/schemas/mls-extraction.ts"
      via: "import MLSExtractionSchema for structured extraction"
      pattern: "MLSExtractionSchema"
    - from: "src/lib/csv-engine.ts"
      to: "papaparse"
      via: "import Papa for CSV parsing"
      pattern: "Papa\\.parse"
    - from: "src/lib/csv-engine.ts"
      to: "src/lib/claude-api.ts"
      via: "calls Claude only for narratives, not scoring"
      pattern: "askClaude|callClaudeWithRetry"
---

<objective>
Harden the generation engine: refactor Claude API to use official SDK with structured output and retry, migrate CSV parsing to Papaparse, ensure scoring is fully deterministic, and add Zod-based config validation.

Purpose: The current engine has reliability issues: max_tokens too low (4096 cuts off responses), JSON parsing from Claude fails on malformed output, hand-rolled CSV parser misses edge cases, and config validation is incomplete.
Output: Robust Claude API module, deterministic CSV pipeline, and Zod validation schemas.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing claude-api.ts exports that other files import -->
From src/lib/claude-api.ts:
```typescript
export async function callClaude(messages: ClaudeMessage[], options: ClaudeOptions): Promise<ClaudeResponse>
export async function askClaude(prompt: string, options?: ClaudeOptions): Promise<string>
export async function askClaudeWithImages(prompt: string, images: {base64: string, mediaType: string}[], options?: ClaudeOptions): Promise<string>
export async function askClaudeWithPDF(prompt: string, pdfBase64: string, options?: ClaudeOptions): Promise<string>
export async function askClaudeWithWebSearch(prompt: string, options?: ClaudeOptions): Promise<string>
```

<!-- csv-engine.ts imports from claude-api -->
From src/lib/csv-engine.ts:
```typescript
import { askClaude } from "./claude-api";
// Uses hand-rolled parseCSVLine() and trimCSVColumns() internally
// computeMatchScore() is already deterministic
// runFullAnalysis() mixes Claude calls with scoring
```

<!-- Existing types used by csv-engine -->
From src/lib/types.ts:
```typescript
export interface CompSale { addr: string; sub: string; sp: number; sf: number; ppsf: number; matchScore: number; ... }
export interface MarketMetrics { medianSoldPrice: number; medianPpsf: number; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Claude API to use Anthropic SDK with structured output and retry</name>
  <files>
    src/lib/claude-api.ts
    src/lib/schemas/mls-extraction.ts
    src/lib/schemas/dashboard.ts
    src/lib/__tests__/claude-api.test.ts
    src/lib/__tests__/config-validation.test.ts
  </files>
  <behavior>
    - callClaudeWithRetry retries up to 3 times on 429 and 5xx status codes
    - callClaudeWithRetry uses exponential backoff with jitter (2^attempt * 1000 + random 0-500ms, max 30s)
    - callClaudeWithRetry does NOT retry on 4xx errors (except 429)
    - Default max_tokens is 16384 (not 4096)
    - extractMLSData uses structured output via output_config.format with zodOutputFormat
    - extractMLSData returns typed object matching MLSExtractionSchema (not raw string)
    - MLSExtractionSchema defines: beds, baths, sqft, yearBuilt, pool, stories, lotSqft, address, subdivision, features array
    - SellDashboardConfigSchema validates all required sell dashboard CONFIG fields
    - BuyerDashboardConfigSchema validates all required buyer dashboard CONFIG fields
    - Backward compatibility: askClaude, askClaudeWithPDF, askClaudeWithImages still work (return strings)
  </behavior>
  <action>
**Install SDK and Zod:**
```bash
npm install @anthropic-ai/sdk zod
```

**Write tests first (RED):**

`src/lib/__tests__/claude-api.test.ts`:
- Mock the `@anthropic-ai/sdk` module (mock `Anthropic` constructor, `messages.create`, `messages.parse`)
- Test "max_tokens": default maxTokens in callClaude is 16384
- Test "structured": extractMLSData calls messages.parse with output_config.format
- Test "retry 429": callClaudeWithRetry retries on 429 status (mock first call throws {status: 429}, second succeeds)
- Test "retry 5xx": callClaudeWithRetry retries on 500 status
- Test "no retry 400": callClaudeWithRetry does NOT retry on 400 (throws immediately)
- Test "backoff": verify delay increases between retries (mock setTimeout or track attempt count)
- Test "backward compat": askClaude returns a string (not structured output)

`src/lib/__tests__/config-validation.test.ts`:
- Test: valid sell config passes SellDashboardConfigSchema validation
- Test: missing required field fails validation with descriptive error
- Test: valid buyer config passes BuyerDashboardConfigSchema validation

**Implement (GREEN):**

1. `src/lib/schemas/mls-extraction.ts`:
   - Define `MLSExtractionSchema` using Zod matching research Pattern 3
   - Export the schema and inferred type

2. `src/lib/schemas/dashboard.ts`:
   - Define `SellDashboardConfigSchema` covering all sell template CONFIG fields
   - Define `BuyerDashboardConfigSchema` covering all buyer template CONFIG fields
   - Define `BuySellDashboardConfigSchema` combining both
   - Export schemas and inferred types
   - Add `validateConfig(config: unknown, type: DashboardType)` function that validates and returns typed result or throws with Zod error details

3. Refactor `src/lib/claude-api.ts`:
   - Replace raw fetch with `@anthropic-ai/sdk` Anthropic client
   - Change default maxTokens from 4096 to 16384
   - Add `callClaudeWithRetry<T>(messages, schema, options)` using `messages.parse()` with `output_config.format: zodOutputFormat(schema)`. Retry logic: 3 attempts, exponential backoff (2^attempt * 1000 + Math.random() * 500, cap 30s), retry on 429 and 5xx only
   - Add `extractMLSData(pdfBase64: string)` using callClaudeWithRetry with MLSExtractionSchema
   - Keep backward-compatible exports: `askClaude`, `askClaudeWithPDF`, `askClaudeWithImages`, `askClaudeWithWebSearch` — these use `messages.create()` (not parse) and return strings
   - `callClaude` remains exported but uses SDK internally instead of raw fetch

**IMPORTANT:** Keep all existing function signatures. Other files import `askClaude` and `callClaude` -- changing their signatures would break imports. The new functions (`callClaudeWithRetry`, `extractMLSData`) are additions.

**Workers compatibility note:** If `@anthropic-ai/sdk` has issues on Cloudflare Workers, the SDK uses fetch internally which should work. Test with `wrangler dev` if possible. Fallback: keep raw fetch approach but add `output_config.format` manually to the request body.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/claude-api.test.ts src/lib/__tests__/config-validation.test.ts</automated>
  </verify>
  <done>
    - Claude API defaults to 16384 max_tokens
    - extractMLSData uses structured output with Zod schema
    - Retry logic handles 429 and 5xx with exponential backoff + jitter
    - Zod schemas validate sell, buyer, and buysell dashboard configs
    - All existing exports (askClaude, callClaude, etc.) still work
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate CSV parsing to Papaparse and separate deterministic scoring from Claude</name>
  <files>
    src/lib/csv-engine.ts
    src/lib/__tests__/csv-engine.test.ts
  </files>
  <behavior>
    - CSV text is parsed using Papa.parse with header:true, skipEmptyLines:true
    - Hand-rolled parseCSVLine() and trimCSVColumns() functions are deleted
    - computeMatchScore() remains deterministic (no Claude call)
    - All metric calculations (median, average, PPSF, DOM) are deterministic
    - Claude is called ONLY for narrative generation (market narrative, comp notes)
    - Claude is called ONLY for comp validation (confirming top picks)
    - runFullAnalysis pipeline: parse CSV -> filter closed sales -> score comps -> calculate metrics -> THEN call Claude for narratives only
    - CSV with quoted fields containing commas parses correctly
    - CSV with empty rows is handled (skipEmptyLines)
  </behavior>
  <action>
**Write tests first (RED):**

Add to `src/lib/__tests__/csv-engine.test.ts`:
- Test "papaparse": parsing a CSV string uses Papa.parse (mock Papa and verify it's called)
- Test "quoted fields": CSV with `"123 Main St, Apt 4"` in a field parses correctly (the comma doesn't split)
- Test "score deterministic": computeMatchScore with same inputs produces same output every time (run 10x, compare)
- Test "metrics deterministic": calculateMetrics with same comp array produces same output (median, avg, etc.)
- Test "no claude in scoring": the scoring/metrics pipeline does not call askClaude (mock askClaude, verify 0 calls during score+metrics phase)
- Test "claude only for narratives": runFullAnalysis calls askClaude only after scoring is complete (verify call order)

**Implement (GREEN):**

Refactor `src/lib/csv-engine.ts`:

1. Replace `parseCSVLine()` and `trimCSVColumns()` with a single `parseCSV(csvText: string)` function using `Papa.parse()`:
   ```typescript
   const result = Papa.parse(csvText, { header: true, skipEmptyLines: true, dynamicTyping: false, transformHeader: h => h.trim() });
   ```
   Filter to closed sales (Status === "C").

2. Delete the `parseCSVLine()` function entirely.
3. Delete the `trimCSVColumns()` function entirely.

4. Ensure `computeMatchScore()` remains pure (no external calls). It should only use subject property data and comp data to calculate a numeric score.

5. Extract metric calculation into a pure function `calculateMetrics(comps: CompSale[])` that computes median, average, PPSF, DOM, etc. deterministically. This should NOT call Claude.

6. Refactor `runFullAnalysis()` into clear phases:
   - Phase 1 (deterministic): Parse CSV -> filter -> score -> sort -> calculate metrics
   - Phase 2 (Claude): Generate narrative text and validate comp selection
   - Return combined result

7. Keep existing exports and function signatures to avoid breaking imports. The refactoring is internal -- the public API stays the same.

**Papaparse is already installed** (v5.4.1 in package.json). No npm install needed.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/csv-engine.test.ts</automated>
  </verify>
  <done>
    - parseCSVLine() and trimCSVColumns() are deleted from csv-engine.ts
    - Papa.parse() handles all CSV parsing
    - Scoring and metrics are fully deterministic (no Claude calls)
    - Claude is called only for narrative generation in Phase 2 of pipeline
    - All CSV engine tests pass (existing + new)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (existing + new)
- `npx tsc --noEmit` — no type errors
- `grep -r "parseCSVLine\|trimCSVColumns" src/lib/csv-engine.ts` returns no results (hand-rolled parsers deleted)
- `grep "16384\|16_384" src/lib/claude-api.ts` confirms new max_tokens default
</verification>

<success_criteria>
- Claude API uses SDK with 16K max_tokens default
- Structured output works via output_config.format + zodOutputFormat
- Retry logic handles 429/5xx with exponential backoff + jitter
- CSV parsing uses Papaparse exclusively (no hand-rolled parser)
- Scoring and metrics are deterministic -- Claude only generates narratives
- Zod schemas validate dashboard configs
- All existing function exports remain backward-compatible
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

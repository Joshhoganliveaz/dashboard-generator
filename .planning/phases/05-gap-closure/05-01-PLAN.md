---
phase: 05-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/wizard/StepMarketData.tsx
  - src/lib/__tests__/supabase-db.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WIZD-10, WIZD-11, PERS-06, WIZD-16]

must_haves:
  truths:
    - "Generation results (comps, market_metrics, pricing_strategy, narratives) persist to sell_data table after step 4 completes"
    - "Deterministic fallback results also persist when Claude generation fails"
    - "Published sell/buysell dashboards render with complete market data at /d/{slug}"
    - "Buyer Back button on step 4 navigates to step 2 without redirect loop"
    - "Sell/buysell Back button on step 4 navigates to step 2 without redirect loop"
  artifacts:
    - path: "src/components/wizard/StepMarketData.tsx"
      provides: "Fixed PATCH URL and Back button targets"
      contains: "fetch(`/api/dashboard/${dashboard.id}`"
    - path: "src/lib/__tests__/supabase-db.test.ts"
      provides: "Green test suite with maybeSingle mock chain"
      contains: "maybeSingle"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes for completed requirements"
      contains: "[x] **WIZD-10**"
  key_links:
    - from: "src/components/wizard/StepMarketData.tsx"
      to: "src/app/api/dashboard/[id]/route.ts"
      via: "fetch PATCH with { sell_data: sellUpdates } payload"
      pattern: "fetch.*api/dashboard.*sell_data"
    - from: "src/components/wizard/StepMarketData.tsx"
      to: "src/hooks/useWizardState.ts"
      via: "goToStep(2) for Back navigation"
      pattern: "goToStep\\(2\\)"
---

<objective>
Fix the four remaining v1.0 gaps: sell data persistence (PATCH fires to non-existent route), buyer/sell Back button redirect loops, and bookkeeping. Also fix 7 pre-existing test failures (2 in supabase-db mock chain, 5 in claude-api mock -- both from stale mocks after code evolved).

Purpose: Close all v1.0 milestone gaps so generation results actually reach the database and wizard navigation works correctly for all dashboard types.
Output: Working sell data persistence, correct Back navigation, green test suite, updated requirement checkboxes.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gap-closure/05-RESEARCH.md

<interfaces>
<!-- Existing API route that StepMarketData should call -->
From src/app/api/dashboard/[id]/route.ts (lines 61-63):
```typescript
if (body.sell_data && typeof body.sell_data === "object") {
  await upsertSellData(id, body.sell_data);
}
```

From src/lib/supabase/db.ts (upsertSellData, lines 63-95):
```typescript
export async function upsertSellData(
  dashboard_id: string,
  data: Partial<Omit<SellData, "id" | "dashboard_id" | "created_at" | "updated_at">>
): Promise<SellData>
// Uses: .from("sell_data").select("id").eq("dashboard_id", id).maybeSingle()
// Then: .update() if exists, .insert() if not
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sell data persistence and Back button navigation</name>
  <files>src/components/wizard/StepMarketData.tsx, src/components/wizard/StepPropertyExtraction.tsx</files>
  <action>
In `src/components/wizard/StepMarketData.tsx`:

1. **Fix PATCH URL (WIZD-10, WIZD-11, PERS-06):** Change line 97 from:
   ```
   fetch(`/api/dashboard/${dashboard.id}/sell-data`, {
   ```
   to:
   ```
   fetch(`/api/dashboard/${dashboard.id}`, {
   ```
   And change line 100 from:
   ```
   body: JSON.stringify(sellUpdates),
   ```
   to:
   ```
   body: JSON.stringify({ sell_data: sellUpdates }),
   ```

2. **Fix silent error swallowing:** Change line 101 from `.catch(() => {` with empty comment to:
   ```
   .catch((err) => {
     console.warn("Failed to persist generation results:", err);
   });
   ```

3. **Fix all three Back buttons (WIZD-16):** Change ALL three `goToStep(3)` calls to `goToStep(2)`:
   - Line 215 (buyer complete/error state Back button)
   - Line 347 (sell/buysell idle state Back button)
   - Line 424 (error state Back button)
   Step 3 was merged into step 2 (per Phase 02-05 decision). All Back buttons should go to step 2 (StepClientInfo) since step 3 immediately redirects to step 4, creating a loop.

4. **Delete orphaned file:** Delete `src/components/wizard/StepPropertyExtraction.tsx` -- it was merged into StepClientInfo in Phase 02-05 and is dead code.

Do NOT create a new `/api/dashboard/[id]/sell-data` route. The existing `/api/dashboard/[id]` PATCH already handles `body.sell_data` via upsertSellData.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>
- PATCH URL targets `/api/dashboard/${dashboard.id}` with `{ sell_data: sellUpdates }` wrapper
- All three goToStep(3) calls changed to goToStep(2)
- Silent .catch replaced with console.warn
- StepPropertyExtraction.tsx deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix test suite and update bookkeeping</name>
  <files>src/lib/__tests__/supabase-db.test.ts, src/lib/__tests__/claude-api.test.ts, .planning/REQUIREMENTS.md</files>
  <action>
1. **Fix supabase-db.test.ts mock chain (2 failing tests):**
   The `upsertSellData` and `upsertBuyData` functions now use select-then-insert/update pattern with `.maybeSingle()` instead of direct `.upsert()`. The mock chain needs updating.

   In the `vi.hoisted` block, add a `mockMaybeSingle` mock function. Update `mockSelect` to also return `{ eq: mockEq }` chain where `mockEq` returns `{ maybeSingle: mockMaybeSingle }` for the existence check path. The tricky part: `mockSelect` is used for both listing (returns `{ order, eq }`) and existence checks (returns `{ eq: ... { maybeSingle } }`). Since these run in different test cases, update `mockEq` to return `{ single: mockSingle, maybeSingle: mockMaybeSingle }` so both patterns work.

   Update the upsertSellData test to:
   - Mock `mockMaybeSingle` to return `{ data: null, error: null }` (row doesn't exist) for the first call
   - Then assert `mockInsert` was called with `{ dashboard_id: "dash-1", address: "123 Main St" }`
   - And `mockFrom` was called with `"sell_data"`

   Same pattern for upsertBuyData test.

2. **Fix claude-api.test.ts (5 failing tests):**
   Read the test file and the current claude-api.ts implementation to understand the mock/implementation mismatch. The tests were written for the old fetch-based API but the code was migrated to `@anthropic-ai/sdk` with `messages.parse()` in Phase 01-03. Update the test mocks to match the current SDK-based implementation pattern. If the tests are testing retry behavior that now lives in the SDK's built-in retry, update assertions accordingly.

3. **Update REQUIREMENTS.md checkboxes:**
   - Change `[ ] **WIZD-10**` to `[x] **WIZD-10**`
   - Change `[ ] **WIZD-11**` to `[x] **WIZD-11**`
   - Change `[ ] **PERS-06**` to `[x] **PERS-06**`
   - Change `[ ] **WIZD-16**` to `[x] **WIZD-16**`

   Also update the Traceability table:
   - PERS-06: change Status from `Pending` to `Complete`
   - WIZD-10: change Status from `Pending` to `Complete`
   - WIZD-11: change Status from `Pending` to `Complete`
   - WIZD-16: change Status from `Pending` to `Complete`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -E "Test Files|Tests"</automated>
  </verify>
  <done>
- All 14 test files pass, 0 failures
- REQUIREMENTS.md shows WIZD-10, WIZD-11, PERS-06, WIZD-16 as complete with [x] checkboxes
- Traceability table shows all four as Complete
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests green (0 failures, was 7 failures before)
2. In StepMarketData.tsx: no occurrences of `/sell-data` URL, no occurrences of `goToStep(3)`
3. StepPropertyExtraction.tsx no longer exists
4. REQUIREMENTS.md has no unchecked v1 requirements
</verification>

<success_criteria>
- Generation results persist to sell_data table via the existing PATCH route (WIZD-10)
- Deterministic fallback results use the same fixed persistence path (WIZD-11)
- Published dashboards render with complete market data since data now reaches DB (PERS-06)
- All Back buttons in StepMarketData navigate to step 2 without redirect loops (WIZD-16)
- Full test suite passes with 0 failures
- All v1.0 requirement checkboxes marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-gap-closure/05-01-SUMMARY.md`
</output>

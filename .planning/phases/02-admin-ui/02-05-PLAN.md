---
phase: 02-admin-ui
plan: 05
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
  - src/components/wizard/StepPreview.tsx
  - src/components/wizard/StepPublish.tsx
  - src/lib/supabase/db.ts
autonomous: false
requirements: [WIZD-12, WIZD-13, WIZD-14]

must_haves:
  truths:
    - "Step 5 shows full dashboard preview in iframe with edit panel"
    - "Team member can edit values directly or give Claude natural language instructions"
    - "Team member can add/remove properties of interest in step 5"
    - "Step 6 shows slug editor (editable before first publish, locked after) and review summary"
  artifacts:
    - path: "src/components/wizard/StepPreview.tsx"
      provides: "Step 5: iframe preview + NL edit + properties of interest"
      min_lines: 80
    - path: "src/components/wizard/StepPublish.tsx"
      provides: "Step 6: slug management + review summary (publish button wired in Phase 3)"
      min_lines: 40
  key_links:
    - from: "src/components/wizard/StepPreview.tsx"
      to: "src/hooks/useGenerateDashboard.ts"
      via: "applyEdit for NL editing"
      pattern: "applyEdit"
    - from: "src/components/wizard/StepPreview.tsx"
      to: "src/lib/supabase/db.ts"
      via: "properties_of_interest CRUD"
      pattern: "properties_of_interest|addPropertyOfInterest|removePropertyOfInterest"
---

<objective>
Build wizard steps 5 and 6: dashboard preview with NL editing and properties of interest management, plus the publish review page with slug editor.

Purpose: Step 5 lets the team member see the generated dashboard and make refinements through natural language editing or by managing properties of interest. Step 6 prepares for publishing with slug management (publish action itself is Phase 3).
Output: Preview step with iframe + edit panel + properties CRUD, and a publish review step with slug editor.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-admin-ui/02-RESEARCH.md
@.planning/phases/02-admin-ui/02-02-SUMMARY.md
@.planning/phases/02-admin-ui/02-04-SUMMARY.md

@src/hooks/useGenerateDashboard.ts
@src/lib/supabase/types.ts
@src/lib/supabase/db.ts
@src/lib/slug.ts

<interfaces>
From src/hooks/useGenerateDashboard.ts:
```typescript
// applyEdit(instruction: string): Promise<boolean>
// - Sends NL instruction to /api/dashboard/edit
// - Returns true if edit applied successfully
// - Updates html in state with edited version
// - isEditing: boolean (loading state during edit)
// - editError: string | null
```

From src/lib/supabase/types.ts:
```typescript
export interface PropertyOfInterest {
  id: string;
  dashboard_id: string;
  address: string;
  price?: number | null;
  listing_url?: string | null;
  photo_url?: string | null;
  notes?: string | null;
  sort_order: number;
  created_at: string;
}
```

Properties of interest CRUD needs to be added to db.ts:
```typescript
// Need to create:
export async function listPropertiesOfInterest(dashboardId: string): Promise<PropertyOfInterest[]>;
export async function addPropertyOfInterest(dashboardId: string, data: {...}): Promise<PropertyOfInterest>;
export async function removePropertyOfInterest(id: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Properties of interest CRUD + StepPreview (iframe + edit + POI)</name>
  <files>src/lib/supabase/db.ts, src/components/wizard/StepPreview.tsx</files>
  <action>
    **1. Extend `src/lib/supabase/db.ts`** with properties of interest CRUD functions:
    - `listPropertiesOfInterest(dashboardId: string): Promise<PropertyOfInterest[]>` -- select from properties_of_interest where dashboard_id matches, order by sort_order
    - `addPropertyOfInterest(dashboardId: string, data: { address: string; price?: number; listing_url?: string; photo_url?: string; notes?: string }): Promise<PropertyOfInterest>` -- insert with auto-incrementing sort_order (max existing + 1)
    - `removePropertyOfInterest(id: string): Promise<void>` -- delete by id
    - Use the same per-request client pattern as existing CRUD functions

    **2. Create `src/components/wizard/StepPreview.tsx`** ("use client"):

    **Props:** Accept wizard state (dashboard data, html from generation, goToStep) + applyEdit, isEditing, editError from useGenerateDashboard.

    **Layout:** Two-panel layout (or collapsible sidebar):

    **Left panel (main): Dashboard Preview**
    - Full-width iframe displaying the generated HTML
    - Write HTML to iframe via `srcdoc` attribute or `postMessage`
    - If no HTML available (user skipped generation or it failed), show message "No preview available. Complete step 4 to generate your dashboard."

    **Right panel (sidebar): Edit Controls**
    Three sections:

    a) **Natural Language Edit** (WIZD-13):
       - Textarea: "Describe what you'd like to change..."
       - "Apply Edit" button calling `applyEdit(instruction)`
       - Loading spinner while `isEditing`
       - Error display from `editError`
       - Example prompts: "Change the listing price to $650,000", "Update the market snapshot with more recent data"

    b) **Properties of Interest** (WIZD-14):
       - List existing properties (loaded from Supabase via `listPropertiesOfInterest`)
       - Each property shows: address, price, notes, remove button
       - "Add Property" form: address (required), price, listing URL, photo URL, notes
       - On add: call `addPropertyOfInterest`, refresh list
       - On remove: call `removePropertyOfInterest`, refresh list
       - Note: Properties of interest display on buyer/buysell dashboards. For sell-only dashboards, this section can be hidden or shown with a note.

    c) **Quick Field Edits** (WIZD-13):
       - Optional: show key editable fields (estimated sale price, pricing strategy) as inline inputs
       - Changes update the CONFIG in the HTML and re-render preview
       - This is a stretch goal -- NL edit covers the requirement. Only implement if time permits.

    **Navigation:**
    - "Back" button: goToStep(4)
    - "Next: Review & Publish" button: goToStep(6)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Step 5 shows dashboard preview in iframe, NL edit panel with Claude editing, and properties of interest CRUD. Properties save to Supabase. Edit applies changes to HTML preview.</done>
</task>

<task type="auto">
  <name>Task 2: StepPublish -- slug editor + review summary</name>
  <files>src/components/wizard/StepPublish.tsx</files>
  <action>
    Create `src/components/wizard/StepPublish.tsx` ("use client"):

    **Props:** Accept wizard state (dashboard data, goToStep, saving).

    **Layout:**

    1. **Review Summary:**
       - Dashboard type badge
       - Client name(s)
       - Property address (sell/buysell) or target areas (buyer)
       - Agent assignment
       - Status: "Draft" (always draft at this point -- publish is Phase 3)
       - Created date, last updated date

    2. **Slug Editor** (SLUG-04, SLUG-05):
       - Show current slug in an editable text input
       - If `dashboard.published_at` is null: slug is editable
       - If `dashboard.published_at` is set: slug is locked (read-only) with lock icon and explanation "Slug is locked after first publish to prevent broken URLs"
       - On edit: validate slug format (lowercase, numbers, hyphens only), check availability via `/api/slug/check?slug=X&excludeId=${dashboard.id}`
       - Show green checkmark if available, red warning if taken (with suggestion)
       - Save slug change to Supabase on blur or explicit save

    3. **Publish Button (placeholder):**
       - Show a disabled "Publish" button with tooltip "Publishing will be available in a future update"
       - This button gets wired in Phase 3 (PUBL-01)
       - For now, show "Save & Return to Library" button that saves any pending changes and navigates to `/`

    **Navigation:**
    - "Back" button: goToStep(5)
    - "Save & Return to Library" button: save, navigate to `/`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Step 6 shows review summary, editable slug (locked after publish), and save/return button. Slug validation and availability check work correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete 6-step wizard flow: type selection -> client info -> MLS extraction -> CSV analysis + comp review + generation -> preview + edit + properties of interest -> slug review</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 -- verify library page shows dashboard cards (or empty state if no dashboards exist)
    2. Click "New Dashboard" -- verify type selection page shows Sell, Buyer, Buy/Sell options
    3. Select "Sell" -- verify wizard opens at step 2 with client info fields
    4. Fill in client name and address, click Next -- verify step 3 shows MLS PDF upload
    5. Skip step 3 (or upload a test PDF if available) -- verify step 4 shows CSV upload
    6. Navigate back to step 2 -- verify data is preserved (WIZD-16)
    7. Return to library (/) -- verify the draft dashboard appears as a card
    8. Click the draft card -- verify wizard reopens with saved data (LIBR-05)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm test` -- full suite still green
- Full wizard flow works end-to-end from type selection through preview
- Back navigation preserves data at every step
- Draft dashboards appear in library and can be resumed
</verification>

<success_criteria>
- Preview iframe shows generated dashboard HTML
- NL edit via Claude updates the preview
- Properties of interest can be added and removed
- Slug is editable before publish, with validation and availability check
- Complete wizard flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-ui/02-05-SUMMARY.md`
</output>
